Добрый день, Александр!

Долго мучался, но что-то я не понимаю.
Делаю страницу результат заказа.
Добавил js контроллер для страницы create_order_result.html. Прописал данный контроллер в index.js и index.html

При нажатии на кнопку подтверждения create_order.js в пост возвращаю id нового созданного заказа
 $http({
            url: contextPath + '/api/v1/orders/confirm',
            method: "POST",
            params: {address: $scope.userinfo.address}
        }).then(function (response) {
            $location.path('/create_order_result?id=' + response.data);
        });

Перевожу на страницу create_order_result

Но вот там в контроллере js не понимаю как получить свой параметр id. Пробовал по разному, не получается.
В примерах нашего магазина не нашёл подобного, в september маркете тоже не нашёл, там th.

Можно прокинуть id через сервис, но я хотел через запрос, как в задании.

Подскажите, что я делаю не так?

С paypakl сейчас начал разбираться, но т.к. и так сильно затянул со сдачей дз, то пока сдаю в таком виде, дальше буду доделывать.